function bankRobbery() {
    const heroes = ['Spiderman', 'Batman', 'Superman', 'Deadpool'];

    function cryForHelp() {
        for (let hero of heroes) {
            console.log(`Help! ${hero}`);
        }
    }
    cryForHelp();
}